#include<iostream>
#include<map>
#include<cstdio>
#include<vector>
using namespace std;
map<int,map<int,char>> record;
vector<vector<char>> rot90(51,vector<char> (51));
vector<vector<char>> rot180(51,vector<char> (51));
vector<vector<char>> rot270(51,vector<char> (51));
int main(){
    int n;
    while(cin>>n){
        if(n==0) break;
        record.clear();
        char mp[n+1][n+1];
        
    }
}